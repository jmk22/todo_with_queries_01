<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>To Do List!</title>
  </head>
  <body>
    @foreach (var task in Model) {
      <p>@task</p>
    }
  </body>

  <form action="/test_add" method="post">
    <button type="submit">Test</button>
  </form>
</html>
